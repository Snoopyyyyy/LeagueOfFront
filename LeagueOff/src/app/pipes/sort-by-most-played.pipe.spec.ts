import { SortByMostPlayedPipe } from './sort-by-most-played.pipe';

describe('SortByMostPlayedPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByMostPlayedPipe();
    expect(pipe).toBeTruthy();
  });
});
